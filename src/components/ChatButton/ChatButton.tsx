import React, { FC } from 'react'

import './ChatButton.scss'

interface IChatButtonProps {}

const ChatButton: FC<IChatButtonProps> = () => {
  return <div className="ChatButton">Чат</div>
}

export default ChatButton
